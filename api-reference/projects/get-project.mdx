---
title: 'Get Project'
openapi: 'GET /v1beta1/projects/{id}'
---

Retrieve detailed information about a specific project by its ID or name.

## Path Parameters

<ParamField path="id" type="string" required>
  Project identifier. Can be either:
  - Project UUID (e.g., `3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c`)
  - Project name/slug (e.g., `data-platform`)
</ParamField>

## Response

<ResponseField name="project" type="object">
  The requested project object.
  
  <ResponseField name="id" type="string">
    Unique identifier for the project (UUID format).
  </ResponseField>
  
  <ResponseField name="name" type="string">
    Unique name/slug for the project.
  </ResponseField>
  
  <ResponseField name="title" type="string">
    Human-readable display name for the project.
  </ResponseField>
  
  <ResponseField name="org_id" type="string">
    ID of the organization this project belongs to.
  </ResponseField>
  
  <ResponseField name="metadata" type="object">
    Custom metadata associated with the project.
  </ResponseField>
  
  <ResponseField name="created_at" type="timestamp">
    Timestamp when the project was created (RFC3339 format).
  </ResponseField>
  
  <ResponseField name="updated_at" type="timestamp">
    Timestamp when the project was last updated (RFC3339 format).
  </ResponseField>
  
  <ResponseField name="members_count" type="integer">
    Number of members in the project.
  </ResponseField>
</ResponseField>

<CodeGroup>

```bash cURL
# Using project ID
curl -X GET 'https://api.frontier.example.com/v1beta1/projects/3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c' \
  -H 'Authorization: Bearer <token>'

# Using project name
curl -X GET 'https://api.frontier.example.com/v1beta1/projects/data-platform' \
  -H 'Authorization: Bearer <token>'
```

```javascript JavaScript
// Using project ID
const projectId = '3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c';
const response = await fetch(`https://api.frontier.example.com/v1beta1/projects/${projectId}`, {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer <token>'
  }
});

const data = await response.json();
console.log(data.project);

// Using project name
const projectName = 'data-platform';
const response2 = await fetch(`https://api.frontier.example.com/v1beta1/projects/${projectName}`, {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer <token>'
  }
});
```

```python Python
import requests

# Using project ID
project_id = '3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c'
response = requests.get(
    f'https://api.frontier.example.com/v1beta1/projects/{project_id}',
    headers={'Authorization': 'Bearer <token>'}
)

data = response.json()
print(data['project'])

# Using project name
project_name = 'data-platform'
response2 = requests.get(
    f'https://api.frontier.example.com/v1beta1/projects/{project_name}',
    headers={'Authorization': 'Bearer <token>'}
)
```

```go Go
package main

import (
    "context"
    "fmt"
    frontierv1beta1 "github.com/raystack/frontier/proto/v1beta1"
)

func getProject(client frontierv1beta1.FrontierServiceClient) {
    // Using project ID
    req := &frontierv1beta1.GetProjectRequest{
        Id: "3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c",
    }
    
    resp, err := client.GetProject(context.Background(), req)
    if err != nil {
        panic(err)
    }
    
    fmt.Printf("Project: %s (%s)\n", resp.Project.Title, resp.Project.Id)
    
    // Using project name
    req2 := &frontierv1beta1.GetProjectRequest{
        Id: "data-platform",
    }
    
    resp2, err := client.GetProject(context.Background(), req2)
    if err != nil {
        panic(err)
    }
}
```

</CodeGroup>

## Response Example

```json
{
  "project": {
    "id": "3f4a5b6c-7d8e-9f0a-1b2c-3d4e5f6a7b8c",
    "name": "data-platform",
    "title": "Data Platform",
    "org_id": "9f256f86-d3c4-4a6e-b89a-1e9a23c5d78e",
    "metadata": {
      "team": "engineering",
      "department": "data",
      "cost_center": "cc-1234"
    },
    "created_at": "2024-01-20T09:15:00Z",
    "updated_at": "2024-02-15T14:30:00Z",
    "members_count": 12
  }
}
```

## Error Responses

<ResponseField name="code" type="string">
  Error code indicating the type of error.
</ResponseField>

<ResponseField name="message" type="string">
  Human-readable error message.
</ResponseField>

### Common Errors

- **400 Bad Request**: Invalid project ID format
- **401 Unauthorized**: Invalid or missing authentication token
- **404 Not Found**: Project not found or you don't have access to it
- **500 Internal Server Error**: Server encountered an unexpected error

## Notes

- You can use either the project UUID or name/slug to retrieve a project
- The service automatically detects whether the provided identifier is a UUID or name
- Access control is enforced - you must have at least read permission on the project
- Disabled projects can still be retrieved if you have the appropriate permissions