---
title: 'Get Billing Balance'
openapi: 'GET /v1beta1/organizations/{org_id}/billing/{id}/balance'
---

<Note>
Retrieve the current credit balance for a billing account. This is useful for prepaid or credit-based billing systems.
</Note>

<RequestExample>

```bash cURL
curl -X GET "https://frontier.example.com/v1beta1/organizations/{org_id}/billing/{id}/balance" \
  -H "Authorization: Bearer <token>"
```

```javascript JavaScript
const response = await fetch(
  'https://frontier.example.com/v1beta1/organizations/{org_id}/billing/{id}/balance',
  {
    method: 'GET',
    headers: {
      'Authorization': 'Bearer <token>'
    }
  }
);
const data = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://frontier.example.com/v1beta1/organizations/{org_id}/billing/{id}/balance',
    headers={'Authorization': 'Bearer <token>'}
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 200
{
  "balance": {
    "amount": 10000,
    "currency": "VC"
  }
}
```

```json 401
{
  "code": 16,
  "message": "Unauthorized - Authentication is required",
  "details": []
}
```

```json 404
{
  "code": 5,
  "message": "Not Found - Billing account not found",
  "details": []
}
```

```json 500
{
  "code": 13,
  "message": "Internal Server Error",
  "details": []
}
```

</ResponseExample>

## Balance Object

<ResponseField name="balance" type="object">
  The balance information for the billing account.
  
  <Expandable title="properties">
    <ResponseField name="amount" type="integer">
      The balance amount in the smallest currency unit (e.g., cents). For virtual credits (VC), this represents the credit balance.
    </ResponseField>
    
    <ResponseField name="currency" type="string">
      Currency code. For credit-based systems, this is typically "VC" (Virtual Credits).
    </ResponseField>
  </Expandable>
</ResponseField>

## Usage Notes

- The balance amount is returned in the smallest unit (e.g., 10000 represents 100.00 in standard currency)
- For virtual credit systems, the currency is "VC" and represents available credits
- A negative balance may indicate debt or usage beyond prepaid credits
- Balance updates occur when:
  - Credits are purchased or added
  - Usage is recorded
  - Refunds are processed
  - Adjustments are made by administrators
